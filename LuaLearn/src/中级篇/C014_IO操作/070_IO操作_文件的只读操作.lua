---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by admin.
--- DateTime: 2021/4/22 9:35
---
--[[
IO分类与使用场合
    Lua I/O库用于读取和处理文件。分为简单模式（和C一样）、完全模式。
    简单模式（simple model)
        拥有一个当前输入文件和一个当前输出文件，并且提供针对这些文件相关的操作。
    完全模式（complete model)
        使用外部的文件句柄来实现。它以一种面对对象的形式，将所有的文件操作定义为文件句柄的方法。
    我们需要在同一时间处理多个文件。我们需要使用file:function_name 来代替 io.function_name方法
    两种模式的适用场合区分：
        简单模式在做一些简单的文件操作时较为合适。但是进行一些高级文件操作的时候，简单模式就显得力不从心，例如同时读取
        多个文件这样的操作，适用完全模式较为合适。
    打开文件操作语句
        file = io.open(filename[,mode])
        mode 的值有：
            r:以只读的方式打开文件，该文件必须存在。（适用：对于配置文件的读取)
            w:打开只写文件，若文件存在则文件长度清为0，即该文件内容会消失。若文件不存在则建立该文件。（适用：
                对于配置文件的写入）
            a：以附加的方式打开只写文件。若文件不存在，则会建立该文件，如果文件存在，写入的数据会被附加到文件尾，
            即文件原先的内容会被保留。[EOF符保留](适用：对于日志文件的追加写入）
            其他：
                r+: 可读写方式打开文件，该文件必须存在。
                w+: 打开可读写文件，若文件存在则文件长度清为零，即该文件内容会消失。若文件不存在则建立该文件。
                a+: 与a类似，但此文件可读可写
                b： 二进制模式，如果文件是二进制文件，可以加上b
                +：  号表示对文件即可以读也可以写
]]

print("典型示例1：以只读的方式打开文件，读取一行")
file = io.open("test1.txt","r")                   --只读，设置输入文件名称
io.input(file)
print(io.read())                                                --输出文件第一行
io.close()                                                      --关闭打开的文件